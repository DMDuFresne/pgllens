"""OAuth support (opt-in, off by default — see Settings.mcp_oauth_enabled).

Pure library code: crypto helpers, in-memory stores, rate limiting, and
client-IP extraction. Nothing here runs unless wired up by the server layer.
"""
